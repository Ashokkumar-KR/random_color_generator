#Random Color Generator

A web app that generates random colors as the background and displays the RGB value.

1.Click "Generate Color" to see a new random color.
2.Text color adjusts for better readability.
3.No installation required; open the HTML file in a web browser.